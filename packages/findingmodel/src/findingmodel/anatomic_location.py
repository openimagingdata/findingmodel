"""Anatomic location models have moved to the anatomic-locations package.

Import from anatomic_locations instead:
    from anatomic_locations import AnatomicLocation, AnatomicRef, AnatomicLocationIndex
"""

raise ImportError(
    "Anatomic location models have moved to the anatomic-locations package. "
    "Use: from anatomic_locations import AnatomicLocation, AnatomicRef, AnatomicLocationIndex"
)
