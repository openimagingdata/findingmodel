[project]
name = "anatomic-locations"
version = "0.1.0"
description = "Anatomic location ontology navigation"
requires-python = ">=3.11"
dependencies = [
    "oidm-common",
    "asyncer>=0.0.2",
    "click>=8.0",
    "duckdb>=1.0",
    "loguru>=0.7",
    "pydantic-settings>=2.9",
    "rich>=13.0",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "openai>=1.0",
    "pydantic-ai-slim>=0.1",
    "oidm-maintenance",  # For create_anatomic_database test utility
]

[project.scripts]
anatomic = "anatomic_locations.cli:main"

[build-system]
requires = ["uv_build>=0.9,<0.10"]
build-backend = "uv_build"

[tool.uv.sources]
oidm-common = { workspace = true }
oidm-maintenance = { workspace = true }
