OPENAI_API_KEY=
# OPENAI_DEFAULT_MODEL=gpt-4o-mini
TAVILY_API_KEY=
# TAVILY_SEARCH_DEPTH=advanced

# DuckDB Database Configuration
# By default, databases are downloaded from manifest.json to platform-specific user data directory
#
# Configuration priority (for both index and anatomic databases):
# 1. If DUCKDB_*_PATH points to existing file (and no URL/hash): use file directly
# 2. If DUCKDB_*_PATH exists AND URL/hash set: verify hash, use file or re-download
# 3. If DUCKDB_*_PATH doesn't exist AND URL/hash set: download using URL/hash
# 4. If nothing specified: download from manifest.json (fallback)
#
# Docker/Production: Pre-mount databases and specify absolute paths
# DUCKDB_INDEX_PATH=/mnt/data/finding_models.duckdb
# DUCKDB_ANATOMIC_PATH=/mnt/data/anatomic_locations.duckdb
#
# Custom Download: Specify URL and hash (both required)
# REMOTE_INDEX_DB_URL=https://custom.host/finding_models.duckdb
# REMOTE_INDEX_DB_HASH=sha256:def456...
# REMOTE_ANATOMIC_DB_URL=https://custom.host/anatomic_locations.duckdb
# REMOTE_ANATOMIC_DB_HASH=sha256:abc123...
#
# Relative paths: Resolved to user data directory
# DUCKDB_INDEX_PATH=my_custom_index.duckdb
# DUCKDB_ANATOMIC_PATH=my_custom_anatomic.duckdb

# Optional: Logfire observability (for eval tracing and debugging)
# Get token from https://logfire.pydantic.dev/ after creating an account
# LOGFIRE_TOKEN=
# DISABLE_SEND_TO_LOGFIRE=false
# LOGFIRE_VERBOSE=false